ação:
    imprimei(1);
    imprimer(2.0);
    imprimei(1+2);
    imprimei(5-1);
    imprimei(10/2);
    imprimei(3*2);
    imprimer(1.5+6.0);
    imprimer(16.5-8.5);
    imprimer(3.0*3.0);
    imprimer(100.0/10.0);
    imprimei(gere_inteiro());
    imprimei(gere_inteiro() / 10000);
    imprimei(gere_inteiro() / gere_inteiro());
    imprimer(gere_real());
    imprimer(gere_real() / 10000.00);
    imprimer(gere_real() / gere_real());
    termine(0)
